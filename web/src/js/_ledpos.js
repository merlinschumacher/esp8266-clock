function initLedSliders() {

    // const ledPosSliders = document.getElementsByClassName("pos-slider");
    // const sliderUpdateFunc = function (event) {
    //     const element = event.target;
    //     const label = element.parentElement.parentElement.querySelector('.slider-chip');
    //     label.textContent = element.value;
    // };
    // Array.from(ledPosSliders).forEach((element) => {
    //     element.addEventListener('input', sliderUpdateFunc, false);
    // });

    // const stripLength = config.ledCount;

};

